\documentclass[DIN, pagenumber=false, parskip=half, a4paper]{scrartcl}

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath, amsfonts, physics}
\usepackage[margin=0pt, landscape]{geometry}
\usepackage{graphics}
\usepackage{color}
\usepackage{url}

\usepackage[
	colorlinks=false,
	pdftitle={Cheatsheet Template},
	pdfauthor={Michael Mueller},
	pdfsubject={Compilation of useful shortcuts.},
	pdfkeywords={Random Software, Cheatsheet}
]{hyperref}


\setlength{\unitlength}{1mm}
\pagestyle{empty}
\setlength{\parindent}{0pt}

\definecolor{mygray}{gray}{.75}
\renewcommand{\dots}{\ \dotfill{}\ }

\title{Cheatsheet Template}
\author{Michael Mueller}
\date{\today}

\renewcommand{\P}{\operatorname{\mathbb{P}}}
\newcommand{\E}{\operatorname{\mathbb{E}}}
\newcommand{\V}{\operatorname{\mathrm{Var}}}

\newcommand{\Ex}[1]{\E \left[ #1 \right]}
\newcommand{\Var}[1]{\V \left[ #1 \right]}

\begin{document}

\begin{picture}(297,210)
	\put(10,200){
		\begin{minipage}[t]{85mm}
			\section*{Probability Notes}
			\paragraph{} \ \\
			\begin{flalign*}
				\Ex{X} &= \mu = \sum_{x_i\in\Omega}x_i\P{x_i}&&\\
				\Ex{g(X)} &= \sum_{x_i\in\Omega}g(x)\,\P{x_i}&&\\
				\Ex{aX+b\,g(X)+c} &= a\Ex{X} + b\Ex{g(X)} + c&&\\
				\Var{X} &= \sigma^2 = \Ex{(X-\mu)^2}&&\\
				\Var{aX+b} &= a^2\Var{X}&&\\
				\Var{X} &= \Ex{X^2} - \mu^2&&
			\end{flalign*}
			\paragraph{Common Probability Distributions} \ \\
			\begin{itemize}
				\item Binomial Distribution:
				      \[
					      X \sim \text{Bin}(n,p) \Rightarrow
					      \begin{cases}
						      \P(X=x) = \binom{n}{x} p^x (1-p)^{n-x} \\
						      \Ex{X} = np                            \\
						      \Var{X} = np(1-p)
					      \end{cases}
				      \]
				\item Poisson Distribution:
				      \[
					      X \sim \text{Poi}(\lambda) \Rightarrow
					      \begin{cases}
						      \P(X=x) = e^{-\lambda} \frac{\lambda^x}{x!} \\
						      \Ex{X} = \lambda                            \\
						      \Var{X} = \lambda
					      \end{cases}
				      \]
				\item Geometric Distribution:
				      \[
					      X \sim \text{Geo}(p) \Rightarrow
					      \begin{cases}
						      \P(X=x) = p(1-p)^{x-1} \\
						      \Ex{X} = \frac{1}{p}   \\
						      \Var{X} = \frac{1-p}{p^2}
					      \end{cases}
				      \]
			\end{itemize}
		\end{minipage}
	}

	\put(105,190.5){
		\begin{minipage}[t]{85mm}
			\paragraph{Transforms} \ \\
			\begin{itemize}
				\item MGF:
				      \begin{flalign*}
					      M(s) &= \Ex{e^{sX}}&&\\
					      \Ex{X^n} &= M^{(n)}(0)&&\\
					      \Var{X} &= M''(0) - M'(0)^2 &&
				      \end{flalign*}
				\item PGF:
				      \begin{flalign*}
					      G(z) &= \Ex{z^X}&&\\
					      \Ex{X} &= G'(1)&&\\
					      \Var{X} &= G''(1) + G'(1) - G'(1)^2 &&
				      \end{flalign*}
			\end{itemize}

			\paragraph{Conditional Probability} \ \\
			\[
				\P_{Y|x}(y) = \frac{\P_{XY}(x,y)}{\P_X(x)}
			\]
		\end{minipage}
	}

	\put(200,189){
		\begin{minipage}[t]{85mm}
			\paragraph{} \ \\
			\paragraph{} \ \\
			\begin{picture}(0,10)
				\linethickness{0,5mm}
				\put(0,0){\color{mygray}\line(1,0){30}}
			\end{picture}

			\footnotesize{
				Template by Michael Müller,\\
				source code can be found at\\
				\url{https://github.com/cmichi/latex-template-collection}
			}
		\end{minipage}
	}
\end{picture}



\end{document}
